/**
 * Thais Ribeiro
 * January 21, 2021
 * Team Project 1
 * My contribution is this class, Item Order
 * this class lets the user choose what item they want and how many
 */
public class ItemOrder
{
    private double orderPrice;
    private String itemName;
    private int quantity;


    public ItemOrder (Item item, int quantity)
    {
        itemName = Item.getName();
        this.quantity = quantity;
    }

    public ItemOrder ()
    {
        this.quantity = 0;
    }

    public String getItem()
    {
        return itemName;
    }

    public int getQuantity()
    {
        return quantity;
    }

    public double getOrderPrice()
    {
        double orderPrice = 0;
        if (quantity % 2 == 0)
        {
            for (int i = 0; i < quantity/2; i++)
            {
                orderPrice += Item.getcost() * 0.75;
            }
            return orderPrice;
        } else {
            for (int i = 0; i < quantity/2; i++)
                orderPrice += Item.getcost() * 0.75;

            return orderPrice + Item.getcost();
        }
    }

}
